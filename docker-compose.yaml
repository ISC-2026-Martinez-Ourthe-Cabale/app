services:
  web:
    build: .
    container_name: ecommerce-web

    ports:
      - "8080:80"

    environment:
      DB_HOST: ${DB_HOST}
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}

    restart: unless-stopped